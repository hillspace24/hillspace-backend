PORT=3000
NODE_ENV=development
MONGODB_URI=mongodb://127.0.0.1:27017/hillspace
FRONTEND_URL=http://localhost:3000
# Extra browser origins (comma-separated). localhost + hillspace.com.ng / www are always allowed.
# CORS_ORIGINS=https://hillspace-app.vercel.app
# Public API origin used in email links (include host only; /api is added automatically)
# On Render this falls back to RENDER_EXTERNAL_URL if unset.
# BACKEND_URL=https://hillspace-backend.onrender.com
# Self-ping /api/health after 9m with no real traffic (keeps Render awake). Off: KEEP_ALIVE_ENABLED=false
# KEEP_ALIVE_ENABLED=true
# KEEP_ALIVE_IDLE_MS=540000
# Optional override (defaults to http://127.0.0.1:$PORT/api/health)
# KEEP_ALIVE_URL=https://hillspace-backend.onrender.com
# Public logo shown at the top of transactional emails (https required)
# EMAIL_LOGO_URL=https://res.cloudinary.com/ar0uptfy/image/upload/f_png,w_240,c_fit,q_auto/hillspace/email-logo.png

JWT_ACCESS_SECRET=change-me-access-secret-min-32-chars
JWT_REFRESH_SECRET=change-me-refresh-secret-min-32-chars
JWT_ACCESS_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# Public register/login. Leave unset/false until launch — only the admin can sign in.
# PUBLIC_AUTH_ENABLED=true

# Seeded on boot (role=admin, email already verified)
ADMIN_EMAIL=hillspace@gmail.com
ADMIN_PASSWORD=hillspace1234!
# ADMIN_PHONE=+2348000000001

CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=
CLOUDINARY_FOLDER=hillspace

# Email — Brevo transactional API (https://app.brevo.com/settings/keys/api)
# Sender must be verified in Brevo (Senders & IPs). Skip boot check: EMAIL_VERIFY_ON_BOOT=false
BREVO_API_KEY=
BREVO_SENDER_NAME=HillSpace
BREVO_SENDER_EMAIL=hillspace24@gmail.com

# Or combined: BREVO_FROM=HillSpace <hello@yourdomain.com>
# Optional: add waitlist signups to a Brevo contact list (Contacts > Lists)
# BREVO_WAITLIST_LIST_ID=2

# --- Payments (optional until you hit checkout/pay endpoints) ---
# App boots fine without these. Missing keys only fail Paystack/AtaraPay routes.
# Paystack (inspection fees + optional escrow collect — NOT third-party escrow)
# https://dashboard.paystack.com/#/settings/developer
PAYSTACK_SECRET_KEY=
PAYSTACK_PUBLIC_KEY=
# PAYSTACK_BASE_URL=https://api.paystack.co

# AtaraPay (true escrow hold / release / refund)
# Staging: https://staging.atarapay.com  Docs: https://plugins.atarapay.com/docs/
# Live: https://app.atarapay.com
ATARAPAY_PUBLIC_KEY=
ATARAPAY_PRIVATE_KEY=
# ATARAPAY_TEST_MODE=true
# ATARAPAY_PAY_URL=http://pay.staging.atarapay.com
# ATARAPAY_API_URL=http://test-api.atarapay.com
# ATARAPAY_IS_MARKETPLACE=false
# Set AtaraPay dashboard Callback URL to:
#   https://YOUR_API_HOST/api/payments/atarapay/callback
# Optional status notifications webhook:
#   https://YOUR_API_HOST/api/payments/atarapay/webhook
# Paystack webhook:
#   https://YOUR_API_HOST/api/payments/paystack/webhook
